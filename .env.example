# API de Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Tailscale — genera una auth key en https://login.tailscale.com/admin/settings/keys
# Usa una "reusable" + "ephemeral" para contenedores temporales,
# o una "reusable" sin ephemeral para que persista en tailnet.
TS_AUTHKEY=tskey-auth-...
